import { PropsWithChildren } from "react"
import { ChildrenProps } from "../../types/CommonTypes"

export const Layout: React.FC<PropsWithChildren<ChildrenProps>> = () => {

    return (
        <>
            Layout!!
        </>
    )

}